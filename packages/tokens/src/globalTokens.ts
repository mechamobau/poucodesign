import type * as Stitches from '@stitches/react';
import {
  shadows,
  radii,
  borderWidths,
  space,
  colors,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
} from './constants';

const globalTokens = {
  borderWidths,
  colors,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  radii,
  shadows,
  space,
};

export default globalTokens;
